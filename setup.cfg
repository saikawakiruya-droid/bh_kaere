[flake8]
max-line-length = 100
exclude = .venv,.git,__pycache__,.mypy_cache,.pytest_cache,build,dist

[tool:pytest]
# Tests live in tests/. Add the project root to the import path so they
# can import the submission modules at the repository root.
testpaths = tests
pythonpath = .
