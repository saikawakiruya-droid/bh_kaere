[flake8]
max-line-length = 100
exclude = .venv,.git,__pycache__,.mypy_cache,.pytest_cache,build,dist,maze_analyzer.py

[mypy]

[mypy-maze_analyzer]
# 課題公式の提供スクリプト。素のまま実行するため型チェック対象外にする。
ignore_errors = True

[tool:pytest]
testpaths = tests
pythonpath = .
